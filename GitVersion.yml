mode: MainLine

branches:
  feature:
    regex: ^(feat|feature)[/-]
    increment: Minor
  hotfix:
    regex: ^(fix|hotfix)[/-]
    increment: Patch
  master:
    regex: ^master$
    increment: Patch
