branches:
  feature:
    regex: ^(feat|feature)[/-]
    increment: Minor
    mode: ContinuousDelivery
  hotfix:
    regex: ^(fix|hotfix)[/-]
    increment: Patch
    mode: ContinuousDelivery
  master:
    regex: ^master$
    increment: Patch
    mode: ContinuousDelivery
